kind:
  allowed:
    - operator
  required: true
  type: string
  default: operator
  meta:
    description: "Which service are you trying to use? Common kinds: `operator`, `job`"

version:
  allowed:
    - "v1"
  required: true
  type: string
  default: v1
  meta:
    description: "Operators may change yaml file schemas from version to version, as well as implementation details. Double check the version to ensure compatibility."

type:
  required: true
  type: string
  default: pii
  meta:
    description: "Type should always be `pii` when using a pii operator"


spec:
  required: true
  schema:
    input_data:
      required: true
      type: dict
      meta:
        description: "This should be indexed by target column."
      schema:
        url:
          required: true
          type: string
          default: data.csv
          meta:
            description: "The url can be local, or remote. For example: `oci://<bucket>@<namespace>/data.csv`"

    output_directory:
      required: true
      schema:
        url:
          required: true
          type: string
          default: result/
          meta:
            description: "The url can be local, or remote. For example: `oci://<bucket>@<namespace>/`"
        name:
          required: true
          type: string
          default: data-out.csv
      type: dict

    report:
      required: true
      schema:
        report_filename:
          required: true
          type: string
          default: report.html
          meta:
            description: "Placed into output_directory location. Defaults to report.html"
        show_rows:
          required: false
          type: number
          default: 25
        show_sensitive_content:
          required: true
          default: false
          type: boolean
      type: dict

    target_column:
      type: string
      required: true
      default: target
      meta:
        description: "Column with user data."

    redactor:
      type: dict
      required: true
      schema:
        detectors:
          required: true
          type: list
          schema:
            type: string
          default: ["phone", "social_security_number"]
          meta:
            description: "default detectors supported by scrubadub"

        spacy_detectors:
          type: dict
          required: false
          schema:
            model:
              type: string
              required: true
              default: en_core_web_trf
            named_entities:
              type: list
              required: true
              default: ["PERSON"]
              schema:
                type: string
          meta:
            description: "Apply spacy NER model to detect the target entities."

        anonymization:
          type: list
          required: false
          schema:
            type: string
          meta:
            description: "Anonylze the selected entities."
  type: dict
