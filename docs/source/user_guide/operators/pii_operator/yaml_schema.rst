===========
YAML Schema
===========

Following is the YAML schema for validating the YAML using `Cerberus <https://docs.python-cerberus.org/en/stable/>`_:

.. literalinclude:: ../common/yaml_schema/piiOperator.yaml
  :language: yaml
  :linenos:
